from fastapi import FastAPI
from pydantic import BaseModel
from typing import TypeVar, Generic, List, Optional

app = FastAPI()

# TODO: TypeVar T を定義してください
# TODO: Define TypeVar T

# TODO: ジェネリックな PagedResponse[T] モデルを作成してください (items, total, page, size)
# TODO: Create generic PagedResponse[T] model (items, total, page, size)

# TODO: User モデルを作成してください (id, name, email)
# TODO: Create User model (id, name, email)

# TODO: Product モデルを作成してください (id, name, price)
# TODO: Create Product model (id, name, price)

# TODO: サンプルデータを定義してください (USERS と PRODUCTS)
# TODO: Define sample data (USERS and PRODUCTS)

# TODO: GET /users/ エンドポイントを実装してください (response_model=PagedResponse[User])
# TODO: Implement GET /users/ endpoint (response_model=PagedResponse[User])

# TODO: GET /products/ エンドポイントを実装してください (response_model=PagedResponse[Product])
# TODO: Implement GET /products/ endpoint (response_model=PagedResponse[Product])
