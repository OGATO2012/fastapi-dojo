from fastapi import FastAPI, Cookie, Response
from typing import Optional

app = FastAPI()

# TODO: GET /set-cookie エンドポイントを実装して "session" クッキーをセットしてください
# TODO: GET /read-cookie エンドポイントを実装して "session" クッキーを読んで返してください
# TODO: Implement GET /set-cookie that sets a "session" cookie
# TODO: Implement GET /read-cookie that reads and returns the "session" cookie
