from fastapi import FastAPI, Query
from typing import List, Optional, Annotated

app = FastAPI()

# TODO: GET /items エンドポイントを実装してください
# クエリパラメータ tags: List[str] = Query(default=[])
# 例: /items?tags=foo&tags=bar → {"tags": ["foo", "bar"]}
# TODO: Implement GET /items
# Query param tags: List[str] = Query(default=[])
# e.g. /items?tags=foo&tags=bar → {"tags": ["foo", "bar"]}

# TODO: GET /ids エンドポイントを実装してください
# クエリパラメータ ids: List[int] = Query(default=[])
# 例: /ids?ids=1&ids=2&ids=3 → {"ids": [1, 2, 3]}
# TODO: Implement GET /ids
# Query param ids: List[int] = Query(default=[])
# e.g. /ids?ids=1&ids=2&ids=3 → {"ids": [1, 2, 3]}
