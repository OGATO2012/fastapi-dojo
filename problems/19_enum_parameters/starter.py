from fastapi import FastAPI
from enum import Enum

app = FastAPI()

# TODO: ModelName という Enum クラスを作成し、"alexnet", "resnet", "lenet" の値を持たせてください
# TODO: GET /models/{model_name} エンドポイントを実装して、モデル名に応じたメッセージを返してください
# TODO: Create a ModelName Enum with values "alexnet", "resnet", "lenet"
# TODO: Implement GET /models/{model_name} that returns a message based on the model name
