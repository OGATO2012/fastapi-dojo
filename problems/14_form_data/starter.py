from fastapi import FastAPI, Form

app = FastAPI()

# TODO: POST /submit エンドポイントを実装して、フォームデータ (name: str, age: int) を受け取り
# TODO: {"name": "...", "age": ...} を返してください
# TODO: Implement POST /submit that accepts form data (name: str, age: int)
# TODO: and returns {"name": "...", "age": ...}
