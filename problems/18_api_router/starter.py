from fastapi import FastAPI, APIRouter

app = FastAPI()

# TODO: items_router という APIRouter を作成し、prefix="/items" を設定してください
# TODO: GET /items/ と GET /items/{item_id} のエンドポイントを router に追加してください
# TODO: router を app に include_router してください
# TODO: Create an APIRouter called items_router with prefix="/items"
# TODO: Add GET /items/ and GET /items/{item_id} endpoints to the router
# TODO: Include the router in the app
