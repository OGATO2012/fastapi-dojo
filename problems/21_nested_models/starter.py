from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

# TODO: Address モデルを作成してください (street: str, city: str, zip_code: str)
# TODO: User モデルを作成してください (name: str, age: int, address: Address)
# TODO: POST /users エンドポイントを実装して User を受け取り、そのまま返してください
# TODO: Create Address model (street: str, city: str, zip_code: str)
# TODO: Create User model (name: str, age: int, address: Address)
# TODO: Implement POST /users that accepts and returns a User
