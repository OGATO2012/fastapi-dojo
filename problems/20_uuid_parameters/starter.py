from fastapi import FastAPI
from uuid import UUID

app = FastAPI()

# TODO: GET /items/{item_id} エンドポイントを実装して
# TODO: item_id を UUID 型として受け取り、{"item_id": str(item_id)} を返してください
# TODO: Implement GET /items/{item_id} that accepts item_id as UUID type
# TODO: and returns {"item_id": str(item_id)}
