# Problem 64: Optional and Required Fields
Create Pydantic models combining required and optional fields.
- Required: username, email. Optional: bio, avatar_url, age. Default: is_active=True
- POST /profiles/ and GET /profiles/sample
