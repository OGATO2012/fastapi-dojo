from fastapi import FastAPI

app = FastAPI()


# TODO: `GET /users/{user_id}` を実装してください。
#       user_id は int 型で受け取り、{"user_id": user_id} を返してください。
# TODO: Implement `GET /users/{user_id}`.
#       Accept user_id as int and return {"user_id": user_id}.


# TODO: `GET /items/{item_name}` を実装してください。
#       item_name は str 型で受け取り、{"item_name": item_name} を返してください。
# TODO: Implement `GET /items/{item_name}`.
#       Accept item_name as str and return {"item_name": item_name}.
