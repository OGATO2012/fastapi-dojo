from fastapi import FastAPI, WebSocket

app = FastAPI()


# TODO: `GET /` エンドポイントを実装してください。`{"message": "WebSocket server ready"}` を返す。
# TODO: Implement `GET /` — return `{"message": "WebSocket server ready"}`.


# TODO: `WS /ws` WebSocketエンドポイントを実装してください。
# TODO: WebSocket接続を受け入れ、テキストメッセージを受信し、"Echo: " + メッセージを返す。
# TODO: Implement `WS /ws` WebSocket endpoint.
# TODO: Accept the connection, receive text messages in a loop, and send back "Echo: " + message.
