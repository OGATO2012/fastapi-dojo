from typing import Optional

from fastapi import FastAPI

app = FastAPI()


# TODO: `GET /items` エンドポイントを実装してください。
#       クエリパラメータ: skip (int, デフォルト 0), limit (int, デフォルト 10), keyword (str | None, デフォルト None)
#       {"skip": skip, "limit": limit, "keyword": keyword} を返してください。
#
# TODO: Implement `GET /items` endpoint.
#       Query params: skip (int, default 0), limit (int, default 10), keyword (str | None, default None)
#       Return {"skip": skip, "limit": limit, "keyword": keyword}.
